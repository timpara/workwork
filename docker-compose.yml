services:
  work-hours:
    build: .
    container_name: work-hours-tracker
    ports:
      - "9090:9090"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/hours.db
    restart: unless-stopped
